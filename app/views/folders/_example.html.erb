<aside id="sidebar" class = "h-100">
  <div class="sidebar-section sidebar-header">
    <div class="sidebar-logo text-center d-flex mt-2">
      <h4 id = "text" class=" p-4 ml-4">Widgets</h4>
    </div>
    <div>
      <div class="card" style="width: 12rem;">
        <img class="card-img-top" src="..." alt="Card image cap">
        <div class="card-body">
          <h5 class="card-title">Card title</h5>
          <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
          <%= link_to "Add a todo list/calendar", folder_sections_path(@folder, section: { section_type: "todo" }), method: "POST" %>
        </div>
      </div>
    </div>
  </aside>
  <li>
    <p><%= link_to "Add a todo list/calendar", folder_sections_path(@folder, section: { section_type: "todo" }), method: "POST" %>
    </p>
    <p><%= link_to "Add a bookmark manager", folder_sections_path(@folder, section: { section_type: "bookmark" }), method: "POST" %>
    </p>
    <p><%= link_to "Add a timer section", folder_sections_path(@folder, section: { section_type: "timer" }), method: "POST" %>
    </p>
  </li>
</div>
<%= link_to "#", new_folder_path %>
</a>
</li>
<li>
  <ul class="nav nav-list collapse" id="submenu1">
    <a href="#" class = "flex-fill pl-5" title="Title">Sub Sub Menu Link</a>              </ul>
</li>
<%# ---------------------------------------------------------------- %>
<%# ---------------------------- %>
</ul>
</aside>
<div class ="wrapper w-100 ml-5 mr-5" >
  <% # Tasks Sections %>
  <div id="section-container-left" class="mr-5">
    <% @sections.each do |section| %>
      <% if section.section_type == "todo" %>
        <div class = "todo-container">
          <div class="page-header">
            <h1>My Tasks</h1>
            <%= image_tag("bar.png") %>
            <div class = "todo-card">
              <ul id="tasks-list">
                <% @tasks[section.id].each do |task| %>
                  <li>
                    <i class="fas fa-grip-lines mr-2"></i>
                    <% if task.completed == "completed" %>
                      <a class = "add" href="<%=  %>"><%= image_tag("checked.png", height: "15", width: "15") %></a>
                    <% else %>
                      <a class = "add" href="<%=  %>"><%= image_tag("empty.png", height: "15", width: "15") %></a>
                    <% end %>
                    <%= link_to task.title, edit_task_path(task) %>
                    <div id = "task" class = "card">
                      <%= simple_form_for task, html: { class: "d-none", data: { edit_movie_target: "form" } } do |f| %>
                        <%= f.input :title %>
                        <%= f.input :description %>
                        <%= f.input :date, as: :date, html5: true %>
                        <%= f.input :is_event %>
                        <%= f.input :completed, collection: Task.completeds.keys %>
                        <%= f.submit %>
                      <% end %>
                    </div>
                    <%= link_to "Delete",
                                          task_path(task.id),
                                          method: :delete,
                                          data: { confirm: "Are you sure?" } %></p>
                  <% if task.completed == "completed" %>
                  <% end %>
                </li>
                <% if task.completed == "completed" %>
                <% end %>
              <% end %>
            </ul>
            <a class = "add" href="<%= new_section_task_path(section) %>"><%= image_tag("add.png") %></a>
          </div>
          <%= link_to "Delete This Section",
                                section_path(section),
                                method: :delete,
                                data: { confirm: "Are you sure?" } %></p>
      </div>
    </div>
          <% end %>
        <% end %>


<%# ---------------------------------------------- %>
  <% if section.section_type == "timer" %>
  <div class = "card" >
    <h1><%= section.section_type %></h1>
    <% @timers[section.id].each do |timer| %>
      <h3 class="text-center"><%= timer.name %></h3>
      <div class="timer">
        <div class="timer__controls">
          <button data-time="0" class="timer__button">Reset</button>
          <form name="customForm" id="customForm">
            <input type="text" name="minutes" placeholder="Enter Minutes">
          </form>
        </div>
        <br>
        <div class="display">
          <h1 class="display__time-left"></h1>
          <p class="display__end-time"></p>
        </div>
        <div class="timer__controls">
          <button data-time="60" class="timer__button">1 Min</button>
          <button data-time="300" class="timer__button">5 Mins</button>
          <button data-time="900" class="timer__button">15 Mins</button>
        </div>
      </div>
    <% end %>
    <% if section.timers.length == 0 %>
      <p><%= link_to "Add a timer", new_section_timer_path(section) %></p>
      <br>
    <% end %>
    <%= link_to "Delete This Section",
                          section_path(section),
                          method: :delete,
                          data: { confirm: "Are you sure?" } %>
        </div>
      <% end %>
      <% end %>
</div>
</div>
</div>



<%# ---------------------------- %>

<div class ="ml-5 mr-5" >
  <% # Tasks Sections %>
  <div id="section-container-left" class="mr-5">
    <% @sections.each do |section| %>
      <% if section.section_type == "todo" %>
        <div class = "todo-container">
          <div class="page-header">
            <h1>My Tasks</h1>
            <%= image_tag("bar.png") %>
            <div class = "todo-card">
              <ul id="tasks-list">
                <% @tasks[section.id].each do |task| %>
                  <li>
                    <i class="fas fa-grip-lines mr-2"></i>
                    <% if task.completed == "completed" %>
                      <a class = "add" href="<%=  %>"><%= image_tag("checked.png", height: "15", width: "15") %></a>
                    <% else %>
                      <a class = "add" href="<%=  %>"><%= image_tag("empty.png", height: "15", width: "15") %></a>
                    <% end %>
                    <%= link_to task.title, edit_task_path(task) %>
                    <div id = "task" class = "card">
                      <%= simple_form_for task, html: { class: "d-none", data: { edit_movie_target: "form" } } do |f| %>
                        <%= f.input :title %>
                        <%= f.input :description %>
                        <%= f.input :date, as: :date, html5: true %>
                        <%= f.input :is_event %>
                        <%= f.input :completed, collection: Task.completeds.keys %>
                        <%= f.submit %>
                      <% end %>
                    </div>
                    <%= link_to "Delete",
                                          task_path(task.id),
                                          method: :delete,
                                          data: { confirm: "Are you sure?" } %></p>
                  <% if task.completed == "completed" %>
                  <% end %>
                </li>
                <% if task.completed == "completed" %>
                <% end %>
              <% end %>
            </ul>
            <a class = "add" href="<%= new_section_task_path(section) %>"><%= image_tag("add.png") %></a>
          </div>
          <%= link_to "Delete This Section",
                                section_path(section),
                                method: :delete,
                                data: { confirm: "Are you sure?" } %></p>
      </div>
    </div>
  <% end %>
  <% if section.section_type == "bookmark" %>
    <div id = "card">
      <h1><%= section.section_type %></h1>
      <% @bookmarks[section.id].each do |bookmark| %>
        <h3><%= bookmark.name %></h3>
        <p><%= bookmark.url %></p>
        <%= link_to "Delete",
                              bookmark_path(bookmark.id),
                              method: :delete,
                              data: { confirm: "Are you sure?" } %></p>
      <br>
    <% end %>
    <p><%= link_to "Add a bookmark", new_section_bookmark_path(section) %><br>
      <%= link_to "Delete This Section",
                            section_path(section),
                            method: :delete,
                            data: { confirm: "Are you sure?" } %></p>
  </div>
<% end %>
<% if section.section_type == "timer" %>
  <div id = "card" >
    <h1><%= section.section_type %></h1>
    <% @timers[section.id].each do |timer| %>
      <h3 class="text-center"><%= timer.name %></h3>
      <div class="timer">
        <div class="timer__controls">
          <button data-time="0" class="timer__button">Reset</button>
          <form name="customForm" id="customForm">
            <input type="text" name="minutes" placeholder="Enter Minutes">
          </form>
        </div>
        <br>
        <div class="display">
          <h1 class="display__time-left"></h1>
          <p class="display__end-time"></p>
        </div>
        <div class="timer__controls">
          <button data-time="60" class="timer__button">1 Min</button>
          <button data-time="300" class="timer__button">5 Mins</button>
          <button data-time="900" class="timer__button">15 Mins</button>
        </div>
      </div>
    <% end %>
    <% if section.timers.length == 0 %>
      <p><%= link_to "Add a timer", new_section_timer_path(section) %></p>
      <br>
    <% end %>
    <%= link_to "Delete This Section",
                          section_path(section),
                          method: :delete,
                          data: { confirm: "Are you sure?" } %>
  </div>
<% end %>
<% end %>
</div>
</div>
</div>
